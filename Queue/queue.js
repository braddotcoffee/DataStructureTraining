/* ----------------------------------------------------------------- */

/**
 * **NOTE** You are not allowed to use the built-in Array.prototype.push() and
 * Array.prototype.shift() functions in order to complete this challenge.
 */

/* ----------------------------------------------------------------- */

/**
 * Adds item to the queue, leaving the original queue untouched
 * @param {Array} Original immutable queue
 * @param {any} Item to add to the queue
 * @returns {Array} New queue containing old items and new item
 */
function enqueue(queue, item) {

}

/**
 * Removes item from the queue, leaving original queue untouched
 * @param {Array} Original immutable queue
 * @returns {Array} First element should be the new queue, second element
 * should be the item removed from the queue
 */
function dequeue(queue) {

}

module.exports = {
  enqueue: enqueue,
  dequeue: dequeue
}
