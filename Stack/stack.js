/* ----------------------------------------------------------------- */

/**
 * **NOTE** You are not allowed to use the built-in Array.prototype.push() and
 * Array.prototype.pop() functions in order to complete this challenge.
 */

/* ----------------------------------------------------------------- */

/**
 * Pushes item to stack, leaving original stack untouched
 * @param {Array} Original immutable stack
 * @param {any} Item to add to stack
 * @returns {Array} New stack containing old items and new item
 */
function push(stack, item) {

}

/**
 * Pops item from stack, leaving original stack untouched
 * @param {Array} Original immutable stack
 * @returns {Array} First element should be the new stack, second
 * element should be the item removed from the stack
 */
function pop(stack) {

}

module.exports = {
  push: push,
  pop: pop
}
